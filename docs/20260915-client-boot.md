# Harness 0.1.6 浏览器启动兼容

版本：`0.6.30-dsh.20260915.2`。

浏览器 Remote 的严格校验器通过 `create()` 返回 JSON 解析器。使用旧 `schema` 字段会被 Harness 0.1.6 注册器拒绝，导致插件启动失败，完整网页停留在 Failed to load plugins。服务端存活和登录接口成功不足以验证浏览器插件可用。

发布验证包含校验器拒绝非 JSON 数据、Harness 0.1.6 真实注册器注册及释放、完整隔离配置登录后的浏览器启动。账号授权和计费规则保持原有实现。
